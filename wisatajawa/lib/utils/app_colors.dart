import 'package:flutter/material.dart';

class AppColors {
  // Background color
  static const Color background = Color(0xFFFAFAFA);

  // Primary color
  static const Color primary = Color(0xFF6200EE);

  // Accent color
  static const Color accent = Color.fromARGB(255, 41, 10, 219);

  // Text colors
  static const Color title = Color(0xFF1F1F1F);
  static const Color subtitle = Color(0xFF666666);
  static const Color hint = Color(0xFFB0B0B0);
  static const Color infoDim = Color(0xFF999999);
  static const Color infoLight = Color(0xFF555555);

  // Surface color
  static const Color surface = Color(0xFFF5F5F5);

  // Border color
  static const Color border = Color(0xFFE0E0E0);

  // Success color
  static const Color success = Color(0xFF51CF66);

  // Error color
  static const Color error = Color(0xFFFF6B6B);

  // Warning color
  static const Color warning = Color(0xFFFFA500);
}
